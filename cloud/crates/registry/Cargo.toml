[package]
name = "registry"
version = "0.1.0"
edition = "2021"
description = "Device registry (metadata only: id/name/platform/public_key/last_seen)."
license = "AGPL-3.0-only"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
