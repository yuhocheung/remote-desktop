[package]
name = "audit"
version = "0.1.0"
edition = "2021"
description = "Audit service (metadata-only event log)."
license = "AGPL-3.0-only"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
