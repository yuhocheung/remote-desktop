{
  "name": "rdcore-web-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser Viewer for rdcore remote desktop (WASM core + native WebRTC)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
